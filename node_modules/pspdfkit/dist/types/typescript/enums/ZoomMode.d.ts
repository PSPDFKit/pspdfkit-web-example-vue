import { $Keys } from "../../utility-types/dist/index";
export declare const ZoomMode: {
    AUTO: string;
    FIT_TO_WIDTH: string;
    FIT_TO_VIEWPORT: string;
    CUSTOM: string;
};
export declare type ZoomModeType = $Keys<typeof ZoomMode>;
