import { $Keys } from "../../utility-types/dist/index";
export declare const ScrollMode: {
    CONTINUOUS: string;
    PER_SPREAD: string;
    DISABLED: string;
};
export declare type ScrollModeType = $Keys<typeof ScrollMode>;
